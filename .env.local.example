# Example environment file for local development.
# Copy this to `.env.local` and replace the placeholders with your real values.
# Do NOT commit `.env.local` to source control. Add it to `.gitignore`.

# GitHub Personal Access Token (read-only public data is sufficient).
# Create a token at https://github.com/settings/tokens (no additional scopes needed for public repos).
# Example value: ghp_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
VITE_GITHUB_TOKEN=YOUR_GITHUB_TOKEN_HERE

# After adding `.env.local` restart the dev server so Vite picks up the value.
# Example:
# npm run dev
